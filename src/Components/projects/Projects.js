import { Container } from "@mui/system";
import { Typography } from "@mui/material";

const Projects = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h4">Projects placeholder</Typography>
    </Container>
  )
}

export default Projects;