import { Container } from "@mui/system";
import { Typography } from "@mui/material";

const Profile = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h4">Profile placeholder</Typography>
    </Container>
  )
}

export default Profile;