import { Container } from "@mui/system";
import { Typography } from "@mui/material";

const Homepage = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h4">Homepage placeholder</Typography>
    </Container>
  )
}

export default Homepage;